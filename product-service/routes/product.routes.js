const router = require('express').Router();

// only user 
router.get('/', (req, res) => {
    // category 
    // price (min , max)
    // Ratings
    // search 
    // pagination (limit and skip)

})


// only for seller 
router.get('/', (req, res) => {
   // only seller 
   // seller id 

})


module.exports = {
    router
}